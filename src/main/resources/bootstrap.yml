spring:
  application:
    name: mercury
  cloud:
    config:
      profile: qa
      label: PRX-85-Email-template
      uri: http://qa.prx.test/config-server
      send-state: true
  jpa:
    defer-datasource-initialization: true
  config:
    activate:
      on-profile: qa
  mail:
    host: smtp.gmail.com #TODO Must be change with Dynamic value
    port: 587 #TODO Must be change with Dynamic value
    username: usuario #TODO Must be change with Dynamic value
    password: clave1 #TODO Must be change with Dynamic value
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            enable: true
        test-conection: true
    freemarker:
      template-loader-path: classpath:/templates
      suffix: .ftlh
